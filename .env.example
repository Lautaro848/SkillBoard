# Usado por `npm run dev` (Vite). En producción estos mismos valores van en
# wrangler.jsonc (vars públicas) y como secretos (`wrangler secret put`) para
# SUPABASE_SERVICE_ROLE_KEY y RESEND_API_KEY — nunca committeados.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
